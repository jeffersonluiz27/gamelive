import styled, { css } from 'styled-components';

export const MadalPerfil = styled.section`
	${() => css`
		.content-react-modal {
			width: 100%;
			max-width: 300px;
			background-color: #fff;
			padding: 3rem;
			position: relative;
			border-radius: 0.3rem;
		}
	`}
`;
